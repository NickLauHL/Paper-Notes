# 📚 Paper Notes - 论文笔记网站

一个简洁学术风格的论文笔记网站，支持分类、标签和搜索功能。

## 🚀 快速开始

### 第一步：创建 GitHub 仓库

1. 登录你的 GitHub 账号
2. 点击右上角 **+** → **New repository**
3. 仓库名称填写 `paper-notes`（或其他你喜欢的名字）
4. 选择 **Public**
5. 点击 **Create repository**

### 第二步：上传文件

**方法一：网页上传（推荐新手）**

1. 在新建的仓库页面，点击 **uploading an existing file**
2. 将本项目所有文件拖拽上传
3. 点击 **Commit changes**

**方法二：Git 命令行**

```bash
git clone https://github.com/你的用户名/paper-notes.git
cd paper-notes
# 将所有文件复制到这个文件夹
git add .
git commit -m "Initial commit"
git push origin main
```

### 第三步：启用 GitHub Pages

1. 进入仓库 **Settings** → **Pages**
2. Source 选择 **Deploy from a branch**
3. Branch 选择 **main** 和 **/(root)**
4. 点击 **Save**
5. 等待几分钟，你的网站就可以访问了！

访问地址：`https://你的用户名.github.io/paper-notes/`

---

## 📝 如何添加论文笔记

### 文件命名规则

在 `_posts` 文件夹中创建文件，文件名格式：

```
年-月-日-标题.md
```

例如：`2024-03-15-attention-is-all-you-need.md`

### 文件内容格式

每篇笔记开头必须有 **Front Matter**（用 `---` 包裹的配置区域）：

```yaml
---
title: "论文标题"
date: 2024-03-15
categories: [深度学习]
tags: [Transformer, NLP, 注意力机制]
paper_info:
  authors: "Vaswani et al."
  venue: "NeurIPS 2017"
  year: 2017
  link: "https://arxiv.org/abs/1706.03762"
---

正文内容从这里开始...
```

**参数说明：**

| 参数 | 必填 | 说明 |
|------|------|------|
| title | ✅ | 笔记标题 |
| date | ✅ | 日期，格式 YYYY-MM-DD |
| categories | ❌ | 分类，如 [深度学习] 或 [CV, NLP] |
| tags | ❌ | 标签，如 [CNN, 图像分类] |
| paper_info | ❌ | 论文详细信息（见下方） |

**paper_info 子参数：**

| 参数 | 说明 |
|------|------|
| authors | 论文作者 |
| venue | 发表会议/期刊 |
| year | 发表年份 |
| link | 论文链接 |

---

## ✏️ Markdown 语法速查

Markdown 很简单，5分钟就能学会！

### 标题

```markdown
## 二级标题
### 三级标题
#### 四级标题
```

### 文字格式

```markdown
**粗体文字**
*斜体文字*
~~删除线~~
`行内代码`
```

### 列表

```markdown
无序列表：
- 第一项
- 第二项
- 第三项

有序列表：
1. 第一步
2. 第二步
3. 第三步
```

### 引用

```markdown
> 这是一段引用的内容
> 可以有多行
```

### 链接和图片

```markdown
[链接文字](https://example.com)

![图片描述](图片地址)
```

### 代码块

````markdown
```python
def hello():
    print("Hello, World!")
```
````

### 表格

```markdown
| 列1 | 列2 | 列3 |
|-----|-----|-----|
| A1  | B1  | C1  |
| A2  | B2  | C2  |
```

### 公式（需要额外配置）

```markdown
行内公式：$E = mc^2$

独立公式：
$$
\frac{1}{n} \sum_{i=1}^{n} x_i
$$
```

---

## 📂 项目结构

```
paper-notes/
├── _config.yml          # 网站配置文件
├── _layouts/            # 页面布局模板
│   ├── default.html
│   └── post.html
├── _includes/           # 可复用组件
│   ├── header.html
│   └── footer.html
├── _posts/              # 📝 论文笔记存放处
│   ├── _TEMPLATE.md     # 笔记模板
│   └── ...
├── assets/
│   ├── css/style.css    # 样式文件
│   └── js/search.js     # 搜索功能
├── categories/index.html
├── tags/index.html
├── search/index.html
├── search.json
├── index.html           # 首页
└── README.md
```

---

## ⚙️ 自定义配置

### 修改网站信息

编辑 `_config.yml`：

```yaml
title: "我的论文笔记"        # 网站标题
description: "记录学习的点滴"  # 网站描述
author: "你的名字"           # 作者名
```

### 修改样式

编辑 `assets/css/style.css` 中的 CSS 变量：

```css
:root {
  --color-primary: #1a365d;    /* 主色调 */
  --color-accent: #3182ce;     /* 强调色 */
  --font-serif: 'Crimson Pro'; /* 正文字体 */
}
```

---

## 🔧 常见问题

### Q: 网站没有更新？

GitHub Pages 部署需要 1-5 分钟，请稍等后刷新。也可以在仓库的 Actions 页面查看部署状态。

### Q: 分类/标签页面是空的？

确保你的笔记文件：
1. 放在 `_posts` 文件夹中
2. 文件名格式正确：`YYYY-MM-DD-title.md`
3. 包含正确的 Front Matter

### Q: 搜索功能不工作？

搜索依赖 `search.json` 文件，确保该文件存在且格式正确。

### Q: 如何添加数学公式支持？

在 `_layouts/default.html` 的 `<head>` 中添加 MathJax：

```html
<script src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>
```

---

## 📄 开源协议

MIT License - 自由使用和修改

---

**Happy Writing! 📖✨**
